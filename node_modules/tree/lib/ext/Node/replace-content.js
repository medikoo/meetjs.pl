// Replace node content

'use strict';

var f       = require('es5-ext/lib/Function/functionalize')
  , forEach = require('es5-ext/lib/List/for-each/call')
  , isList  = require('es5-ext/lib/List/is-list')
  , clone   = require('es5-ext/lib/List/clone').call

  , empty   = require('./empty').call;

module.exports = f(function (list) {
	var i, n;
	if (!isList(list)) {
		list = arguments;
	} else {
		list = clone(list);
	}

	empty(this);
	forEach(list, this.appendChild, this);
	return this;
});
