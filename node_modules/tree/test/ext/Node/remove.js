'use strict';

module.exports = function (t, a) {
	var p, c;
	t = t.call;

	p = document.createNode();
	c = p.appendChild(document.createNode());

	a(t(c), c, "Return value");
	a(c.parentNode, null, "No parent");

	a(t(c), c, "No error if not in element");
};
