'use strict';

var node     = require('../../lib/core/node')()
  , document = require('../../lib/core/document')(node)

module.exports = function (t, a) {
	var o = Object.create(t(document)), doc;

	doc = o.createDocument();
	a(doc.implementation, o, "Document implementation");
	a(doc.ownerDocument, doc, "Document ownerDocument");
	a(doc.childNodes.length, 1, "Document childNodes");

	a.not(doc, o.createDocument(), "Different document");
};

