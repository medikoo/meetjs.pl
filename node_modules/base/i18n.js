'use strict';

var i18n = function (name) {
	if (!i18n.messages[name]) {
		// console.warn("Not found i18n message for '" + name + "'");
		return name;
	}
	return i18n.messages[name];
};
i18n.messages = {};
module.exports = i18n;
