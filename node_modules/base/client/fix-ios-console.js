'use strict';

var contains = require('es5-ext/lib/List/contains').call

  , join, _native, _nativeWarn;

if (contains(navigator.userAgent, 'iPhone') ||
	 contains(navigator.userAgent, 'iPad')) {
	join = require('es5-ext/lib/List/join/call');

	_native = console.log;
	console.log = function () {
		_native.call(console, join(arguments, " "));
	};

	_nativeWarn = console.warn;
	console.warn = function () {
		_nativeWarn.call(console, join(arguments, " "));
	};
}
