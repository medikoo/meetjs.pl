'use strict';

var peek        = require('es5-ext/lib/List/peek').call
  , navigator   = require('navigator')(window)

module.exports = navigator;

if (peek(location.pathname) !== '/') {
	navigator.load(location.pathname + '/' + location.search + location.hash);
}
