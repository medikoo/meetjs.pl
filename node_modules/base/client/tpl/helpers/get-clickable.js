'use strict';

module.exports = function (event, limit) {
	var el = event.target;
	while (el && (el.nodeName.toLowerCase() !== 'a')) {
		if (el !== limit) {
			el = el.parentNode;
		} else {
			el = null;
		}
	}
	return el;
};
